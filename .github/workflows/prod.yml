name: Prod - Promotion Only
on:
  push:
    branches:
      - prod
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Prod workflow
        run: echo "Prod workflow triggered successfully!"
