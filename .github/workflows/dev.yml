name: Dev - Artifact-First Pipeline
on:
  push:
    branches:
      - dev
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Test workflow
        run: echo "Dev workflow triggered successfully!"
